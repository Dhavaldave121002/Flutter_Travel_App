// phonepay.dart
import 'package:flutter/material.dart';

class PhonePePage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('PhonePe')),
      body: Center(child: Text('PhonePe Payment Page')),
    );
  }
}
